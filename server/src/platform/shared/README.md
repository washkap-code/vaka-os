# Shared Platform Types

Shared contracts cover clocks, identifiers, logging, and JSON-safe values.
They are deliberately provider-neutral so modules can use deterministic test
implementations without importing infrastructure code.
