# Identity Platform Contract

Identity exposes authenticated actor, tenant, session, and permission context
to future modules without importing the current Express authentication
implementation. It is a contract boundary only in P1-001.
