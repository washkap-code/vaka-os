# Platform Types Namespace

The types namespace contains small, reusable type helpers and explicit
tenant-scope assertions shared by Platform contracts. It must not become a
domain-model dumping ground: ERP fields belong to their owning module.
